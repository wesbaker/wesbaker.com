#!/bin/sh

set -eu

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

if [ ! -d node_modules ]; then
  echo "pre-commit: run 'npm install' before committing." >&2
  exit 1
fi

echo "pre-commit: building and validating internal links..."
npm run --silent check
